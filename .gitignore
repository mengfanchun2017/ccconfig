# Claude Code 本地配置（包含 API Key，不同步）
.claude.json

# GetNote 等外部工具配置（包含 API Key）
openclaw.json

# MCP 鉴权信息文件（包含 Key/Token，本地维护）
config/mcpidentity.json

# auto-sync 运行文件
.auto-sync.log
.auto-sync.pid

# 可选：本地环境变量文件
# .env
