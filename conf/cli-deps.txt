# CLI 工具依赖清单 — skill 运行所需的命令行工具
#
# 格式：<package>  <manager:version>  <required-by>
# manager: npm | go | pip | apt
# init-skill.sh sync 时自动安装缺失的依赖
#
# 更新：改版后上游 CLI 有新版本时：
#   npm update -g <package>       # npm 包
#   go install ...@latest         # go 包

@getnote/cli  npm:latest  f-getnote
