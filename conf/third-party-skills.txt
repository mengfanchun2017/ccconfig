# 第三方 skill 清单（user-managed via npx skills add）
#
# 格式：每行 `<source>  <skill-name>`，注释以 # 开头
# 装法：bash init-skill.sh sync（自动 do_install_third_party，幂等）
# 更新：npx skills update -g -y 或 bash scripts/update-third-party-skills.sh
# diff：bash init-skill.sh diff（检测清单 vs 实际安装的 drift）
#
# WHY 用 npx skills 不用 marketplace install（2026-06-06 重构）：
#   - marketplace install 暴露 plugin 前缀（mattpocock-skills:caveman）和 🔒 锁
#   - dialog 太噪音，用户无法单独 toggle 每个 skill
#   - npx skills 装到 ~/.agents/skills/，自动 symlink 到 ~/.claude/skills/（干净显示）
#   - 缺点：版本不自动跟（需手动 update），但用户可接受（周期性 update）

# ===== 社区 skill：对话增强（mattpocock 中文版）=====
# 用途：caveman 压缩输出、diagnose 调试、grill-me 追问设计、improve-codebase 重构、write-a-skill 建 skill
vinvcn/mattpocock-skills-zh-CN  caveman
vinvcn/mattpocock-skills-zh-CN  diagnose
vinvcn/mattpocock-skills-zh-CN  grill-me
vinvcn/mattpocock-skills-zh-CN  improve-codebase-architecture
vinvcn/mattpocock-skills-zh-CN  write-a-skill

# ===== MCP server：Get笔记（getnote）=====
# MCP: @getnote/mcp (npm)，已装 v1.5.2，settings.json mcpServers.getnote 管理配置
# 27 tools 全覆盖（含图片上传），不再需要 CLI skills

# ===== OfficeCLI skill：Office 文档生成（PPTX/DOCX/XLSX）=====
# CLI: officecli (curl install)，已装 v1.0.128，option-officecli/init.sh 管安装
# officecli = 命令参考 + load_skill 机制（pptx/word/excel 子技能内置 CLI 二进制，运行时加载）
# f-pptx/f-docx/f-xlsx = 个人设计系统 + 个人模板，不重复 officecli 的命令参考
# 详见 docs/adr/0004-officecli-skill-architecture.md
iofficeai/officecli  officecli
