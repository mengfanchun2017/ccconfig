{
    "description": "Claude Code 全局配置 / 应用状态文件（官方文档里叫 ~/.claude.json）。这里只放 user scope 的 mcpServers 与 Claude Code 自己维护的状态 —— 它【不是】settings 文件。",
    "mcpServers": {
        "tavily": {
            "command": "npx",
            "args": ["-y", "tavily-mcp"],
            "env": {
                "TAVILY_API_KEY": "<your-tavily-key>"
            }
        },
        "getnote": {
            "command": "npx",
            "args": ["-y", "@getnote/mcp"],
            "env": {
                "GETNOTE_API_KEY": "<your-getnote-api-key>",
                "GETNOTE_CLIENT_ID": "<your-getnote-client-id>"
            }
        },
        "exa": {
            "type": "http",
            "url": "https://mcp.exa.ai/mcp",
            "headers": {
                "x-api-key": "<your-exa-api-key>"
            }
        },
        "supabase": {
            "command": "npx",
            "args": ["-y", "@supabase/mcp-server-supabase", "--project-ref", "<your-project-ref>"],
            "env": {
                "SUPABASE_ACCESS_TOKEN": "<your-supabase-token>"
            }
        }
    }
}
