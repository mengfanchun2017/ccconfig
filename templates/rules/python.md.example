# Python 管理规范

## 版本
- 使用系统 Python 3（Ubuntu 24 apt 版本），不在终端用 deadsnakes/conda
- `python3` = 系统默认，不要自己编译安装

## 包管理
- **原则**: 能用 apt 安装的优先 apt，apt 没有的用 pip3
- pip3 安装到 user site: `~/.local/lib/python3.x/site-packages/`
- apt 安装到系统目录
- 如果 apt 和 pip 都有同一个包，pip 版本会优先（user site 路径优先）

## 依赖清单
- 统一维护依赖清单文件（如 `conf/python-requirements.txt`）
- 恢复: `pip3 install -r <requirements.txt>`

## 安装权限
- Claude 可以在任务需要时自主 `pip3 install` 新包，不询问用户
- 安装后自动更新依赖清单并标注新增

## 升级
- 定期升级: `pip3 install --upgrade -r <requirements.txt>`
- 生成最新清单: `pip3 freeze --user > <requirements.txt>`

## 禁止
- 禁止用 `sudo pip3 install`（sudo 会装到系统 site-packages，与 apt 冲突）
- 禁止创建 venv 来做临时任务（太复杂，直接 pip3 install）