# gh auth 双选项设计

> 2026-07-30 决定：ccconfig 初始化中 gh auth 登录改为 A/B/C 菜单

## 背景

两台机器上都遇到 `gh auth status` 报未登录，但 `~/.config/gh/hosts.yml` 是空 `{}`。
原 bootstrap-gh-auth.sh 的 Step 3 逻辑是：有代理→Web OAuth；无代理→fine-grained PAT 交互。
init-ccprivate-repo.sh 的 check_gh_auth() 逻辑类似，但多了一个 SSH key 跳过选项。

两个文件各自实现 auth 流程，但设计散乱——有代理/无代理的判断、fine-grained vs classic PAT 的混合说明、Web OAuth 失败后自动回退，用户不清楚发生了什么。

## 讨论结论

| 维度 | PAT 粘贴（A） | Web OAuth（B） |
|------|--------------|---------------|
| 持久性 | No-Expiration 永久有效 | 有过期时间，需重认证 |
| 跨机器 | 同一个 token 多机器用 | 每台独立登录 |
| 存储 | `~/.config/gh/hosts.yml` 600，明文 | 同左，但 token 是短期 |
| 安全性 | 等同于 SSH private key 级别 | 短期泄露影响小 |

A 默认（推荐），B 备选，C（仅 init-ccprivate-repo.sh）SSH only 跳过。

**关键约束**：
- Token 仅存本地 `~/.config/gh/hosts.yml`，**不在 ccprivate 同步**
- 用户可自己记录 token 用于其他终端
- 不引入加密/密钥管理基础设施（KMS、git-crypt 等）

**为什么不存 ccprivate**：任何远程存储 plaintext secret 都有风险，为一把 token 引入加密流程得不偿失。

## 改动文件

- bootstrap-gh-auth.sh（Step 3）：移除代理检测+自动回退，改为 A/B 菜单
- init-ccprivate-repo.sh（check_gh_auth）：移除旧三选项，改为 A/B/C 菜单（多一个 SSH skip）
- BOOTSTRAP.md：同步描述文本

## 未改动

- GH_TOKEN/GITHUB_TOKEN 环境变量自动登录保留（CI/无交互场景）
- SSH key 跳过逻辑保留（git push 不依赖 gh auth）
- init-ubuntu.sh 的 SSH 密钥生成逻辑不变