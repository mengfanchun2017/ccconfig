# Rule 参考模板

本目录放置 `.example` 模板，是 [ccprivate 全局 rules](../../../ccprivate/rules/) 的公开参考版本。

**与旧版本的区别：**
- 不再双向同步，不再检查差异
- 模板只是参考，用户改 ccprivate 后模板不跟随
- 不包含隐私/个人/设计决策类内容

**用法：** 复制到 `~/.claude/rules/`（或 `ccprivate/rules/`）后按需修改。