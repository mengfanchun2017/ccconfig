#!/bin/bash
# Claude Code - Claude Code 安装配置入口
# 功能：安装 Claude Code 并配置自定义 LLM API
#
# 使用方法（从 git 目录运行）：
#   bash claude-config/init02claude
#
# 或直接运行脚本：
#   bash claude-config/scripts/bash/init02claude.sh

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
"$SCRIPT_DIR/scripts/bash/init02claude.sh"
